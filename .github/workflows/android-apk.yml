# ARZON — Android APK'ni brauzerdan (terminalsiz) chiqarish.
#
# Ishlatish (hammasi brauzerda):
#   1. expo.dev'da bepul hisob oching.
#   2. expo.dev -> (o'ng yuqori) Account settings -> Access tokens -> "Create token"
#      -> tokenni nusxa oling.
#   3. GitHub'da: bu repo -> Settings -> Secrets and variables -> Actions ->
#      "New repository secret" -> Name: EXPO_TOKEN -> tokenni qo'ying -> Save.
#   4. GitHub'da: yuqoridagi "Actions" bo'limi -> chapdan "ARZON Android APK" ->
#      "Run workflow" tugmasi -> yashil "Run workflow".
#   5. 15-20 daqiqada tayyor. APK havolasi: expo.dev -> loyihangiz -> "Builds".
#
# Eslatma: birinchi marta ishga tushganda Expo loyihani avtomatik yaratadi
# (app.json'dagi "owner" + "slug" bo'yicha). "owner" hali qo'yilmagan bo'lsa,
# app.json'ni GitHub'ning o'zida (qalam belgisi bilan) tahrirlab, "expo" ichiga
#   "owner": "SIZNING_EXPO_LOGININGIZ"
# qatorini qo'shing (terminalsiz).

name: ARZON Android APK

on:
  workflow_dispatch: # brauzerdan "Run workflow" tugmasi bilan qo'lda ishga tushadi

jobs:
  build-apk:
    runs-on: ubuntu-latest
    steps:
      - name: Kodni olish
        uses: actions/checkout@v4

      - name: Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: EAS'ni sozlash (Expo token bilan)
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: Token borligini tekshirish
        run: |
          if [ -z "${{ secrets.EXPO_TOKEN }}" ]; then
            echo "❌ EXPO_TOKEN topilmadi."
            echo "   GitHub -> Settings -> Secrets and variables -> Actions"
            echo "   -> New repository secret -> Name: EXPO_TOKEN"
            exit 1
          fi
          echo "✅ EXPO_TOKEN bor. Expo hisobi:"
          eas whoami

      - name: Kutubxonalarni o'rnatish
        working-directory: mobile
        run: npm install

      # Expo loyihasini avtomatik yaratadi/bog'laydi (app.json'ga projectId qo'shadi).
      # Bu bo'lmasa eas build "projectId yo'q" deb to'xtaydi (birinchi build xatosi).
      - name: Expo loyihasini bog'lash (projectId)
        working-directory: mobile
        run: eas init --force --non-interactive

      # Build buyurtmasi Expo'ga jo'natiladi. Chiqqan havola ushlab olinadi va
      # pastda GitHub sahifasining o'ziga yoziladi (Expo'ni qidirib yurmaslik uchun).
      - name: APK build (Expo bulutida)
        working-directory: mobile
        run: |
          set -o pipefail
          eas build --platform android --profile preview \
            --non-interactive --no-wait 2>&1 | tee build.log
          echo "BUILD_URL=$(grep -oE 'https://expo\.dev/[^ ]*builds/[a-z0-9-]+' build.log | head -1)" >> "$GITHUB_ENV"

      - name: Build havolasi (shu yerda)
        if: always()
        run: |
          {
            echo "## 📱 ARZON — Android APK"
            echo ""
            if [ -n "$BUILD_URL" ]; then
              echo "✅ Build Expo'ga jo'natildi. Holatini shu havolada ko'ring:"
              echo ""
              echo "### 👉 [$BUILD_URL]($BUILD_URL)"
              echo ""
              echo "15–20 daqiqadan so'ng o'sha sahifada **Download** tugmasi chiqadi (.apk)."
              echo "Faylni Telegram'ga tashlasangiz — odamlar o'rnatadi."
            else
              echo "⚠️ Build havolasi topilmadi — yuqoridagi \"APK build\" qadamini ochib,"
              echo "qizil yozuvni ko'ring."
            fi
          } >> "$GITHUB_STEP_SUMMARY"
          echo "Build havolasi: ${BUILD_URL:-topilmadi}"
