# Cloud (Render) uchun backend bog'liqliklari — yengil to'plam.
# Faqat backend web-xizmati uchun kerak bo'lganlari (chromadb/anthropic emas —
# ular ixtiyoriy va og'ir; kerak bo'lса keyin qo'shiladi).
fastapi==0.139.2
uvicorn==0.51.0
SQLAlchemy==2.0.51
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2
httpx==0.28.1
# PostgreSQL (Neon) drayveri — doimiy baza uchun.
psycopg2-binary==2.9.10
# Telegram botlar (webhook rejimi — bulutда, backend ichida ishlaydi).
# [job-queue] — kunlik hisobot uchun (APScheduler).
python-telegram-bot[job-queue]==22.8
# Gemini Flash — chek/rasm tahlili (ACOM coin). Kalit bo'lmasa ishlamaydi,
# lekin backend baribir ishlaydi (super-admin qo'lда tekshiradi).
google-genai==1.16.1
